version: '3'
services:
  aipo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aipo
    cap_add:
      - ALL
    ports:
      - "8080:80"
    hostname: aipo.example.com
    restart: always
    volumes:
      - postgresdata:/srv/aipo/postgres/data
volumes:
  postgresdata: {}
#networks:
#  default:
#    external:
#       name: webproxy
