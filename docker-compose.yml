version: '2'
services:

  https-portal:
    image: steveltn/https-portal:latest
    ports:
      - '80:80'
      - '443:443'
    links:
      - aipo
    restart: always
    environment:
      DOMAINS: 'aipo.example.com -> http://aipo'
      STAGE: 'production'
      # FORCE_RENEW: 'true'
  aipo:
    image: diorama/aipo:latest
#    links:
#      - aipodb
#    environment:
#      hostname: "https://aipo.example.com"
    volumes:
       - /srv/aipo/data:/srv/aipo/postgres/data:ro
    ports:
    # Any port 8080.
      - "8080:80"
    restart: always

#  aipodb:
#    image: postgres:9.5
#    ports:
#      - "5432:5432"
#    environment:
#      POSTGRES_USER: docker
#      POSTGRES_PASSWORD: docker
#      POSTGRES_DB: docker
#    restart: always
#      volumes:
#        - /srv/aipo/data:/srv/aipo/postgres/data
